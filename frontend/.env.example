# Base URL of the backend API. Baked into the bundle at build time (vite build),
# so set it BEFORE building: dev = http://localhost:5000/api,
# production = https://your-domain.com/api
VITE_API_URL=http://localhost:5000/api